/// <reference types="react" />
import * as React from 'react';
import { SharedRenderProps } from '../types';
export declare const Media: React.SFC<SharedRenderProps<MediaProps>>;
export interface MediaProps {
    matches: boolean;
}
